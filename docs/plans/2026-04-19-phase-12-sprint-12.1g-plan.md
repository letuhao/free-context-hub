---
id: PLAN-PHASE12-S121G
date: 2026-04-19
module: Phase12-Sprint12.1g
phase: PHASE_12
spec_ref: SPEC-PHASE12-S121G
design_ref: DESIGN-PHASE12-S121G
---

# Plan — Phase 12 Sprint 12.1g — TEI external rerank integration

## T0 pre-flight
- `docker compose ps` — check mcp + db up
- `curl http://localhost:3001/api/lessons?... limit=1` — API OK
- `lesson_access_log` COUNT = 90
- TEI image already pulled: `docker images | grep text-embeddings-inference` → expect 1 row

## T1 — docker-compose.yml `tei-rerank` service

**T1.1 Edit docker-compose.yml:**
- Add `tei-rerank` service block (per DESIGN §1).
- Add `tei_model_cache` to top-level `volumes:` block.
- Add `tei-rerank` to `mcp` service `depends_on`.

**T1.2 Verify compose config:**
- `docker compose config | grep -A 10 tei-rerank` shows service.
- `docker compose config | grep tei_model_cache` shows volume.
- `docker compose config --services` includes `tei-rerank`.

**T1.3 Start TEI service:**
- `docker compose up -d tei-rerank`
- Monitor logs: `docker compose logs -f tei-rerank | grep -E "Ready|Error"`.
- Expected: ~4-5min for model download on first start (volume was empty from experimental run).
- Wait for "Ready" log line.

**T1.4 Smoke-test /rerank:**
- `curl -s -X POST http://localhost:8080/rerank -H 'Content-Type: application/json' -d '{"query":"pg uuid casing","texts":["pg UUID cast RETURNING is canonical","how to render a unicorn"]}'`
- Verify response: `[{"index":0,...}, {"index":1,...}]` with index 0 having higher score.

## Commit 1 (C1) — infra + docs
Files: spec + design + plan + docker-compose.yml.
Message: `Phase 12 Sprint 12.1g spec + design + plan + tei-rerank docker service`.

## T2 — src/env.ts enum update

**T2.1 Add `'api'` to RERANK_TYPE enum:**
```typescript
RERANK_TYPE: z.enum(['generative', 'cross-encoder', 'api']).optional().default('generative'),
```

**T2.2 Verify:** `npx tsc --noEmit` clean.

## T3 — src/services/lessons.ts new function + dispatch

**T3.1 Add `rerankExternalApi()` function** (per DESIGN §4) after `rerankCrossEncoder()` (~line 660).

**T3.2 Update `rerankLessons()` dispatch** (per DESIGN §5):
- Check `RERANK_TYPE === 'api'` FIRST (before the `model` fallback logic).
- Call `rerankExternalApi` → return order.

**T3.3 Verify:** `npx tsc --noEmit` clean. `npm test` passes (existing 231).

## T4 — Unit tests

**T4.1 Add test group to `src/services/salience.test.ts`** (or create new test file if cleaner). Tests:
1. `flag=true, happy path`: mocked fetch returns `[{index:2,score:0.5},{index:0,score:0.3},{index:1,score:0.1}]` → function returns `[caller_idx_of_2, caller_idx_of_0, caller_idx_of_1]`.
2. `HTTP error (500)`: returns original `candidates.map(c => c.index)`.
3. `Network error (fetch throws)`: returns original order.
4. `Empty response array`: returns original order.

Match the `mockPool` pattern — use `const originalFetch = global.fetch; global.fetch = mockFetch; try { ... } finally { global.fetch = originalFetch; }`.

**T4.2 Verify:** `npm test` 231 + new tests pass (expect 234-235).

## Commit 2 (C2) — code
Files: src/env.ts, src/services/lessons.ts, tests file.
Message: `Phase 12 Sprint 12.1g — rerankExternalApi() + RERANK_TYPE=api dispatch + unit tests`.

## T5 — Baseline run

**T5.1 Recreate mcp with RERANK_TYPE=api:**
- Will need mcp image rebuild first — `docker compose build mcp` (code change needs baking in).
- `RERANK_TYPE=api LESSONS_SALIENCE_NO_WRITE=true docker compose up -d --force-recreate mcp`
- Wait for ready.
- Verify env: `docker compose exec mcp node -e "console.log('RT:', process.env.RERANK_TYPE, 'BASE:', process.env.RERANK_BASE_URL || '(default)')"`.

**T5.2 Run baseline:**
- `npm run qc:baseline -- --tag sprint-12.1g-tei-bge --samples 1 --surfaces lessons` (background + Monitor).
- Expect: ~1min elapsed (40 queries × ~1s per query via CPU TEI).
- Verify: archive exists, `per_query.length=40`, `errors=0`, `lesson_access_log` COUNT=90.

**T5.3 Quick quality check:** aggregate MRR / nDCG@10. Compare to generative (1.0/0.9724), gte (0.9538/0.9237), no-rerank (0.9198/0.9100).

## T6 — Determinism check

**T6.1 Recreate mcp with same env:**
- `RERANK_TYPE=api LESSONS_SALIENCE_NO_WRITE=true docker compose up -d --force-recreate mcp`
- Verify env unchanged.

**T6.2 Run baseline:**
- `npm run qc:baseline -- --tag sprint-12.1g-tei-bge-repeat --samples 1 --surfaces lessons`
- Compare to T5.2 output: 0/40 query diffs.

## T7 — Analysis + summary doc

**T7.1 5-way comparison table:** api (new), gte, generative, no-rerank, (optionally new vs repeat determinism).

**T7.2 Per-group nDCG@10** — 6 groups × all rerank modes.

**T7.3 Summary doc** `docs/qc/baselines/2026-04-19-sprint-12.1g-summary.md` with:
- Top-line recommendation (per DESIGN §9 matrix).
- 5-way aggregate table.
- Per-group breakdown.
- Determinism verdict.
- If winner → recommend src/env.ts default change.

## T8 (conditional) — src/env.ts default change

If TEI+bge wins per decision matrix:
- `RERANK_TYPE: z.enum(['generative', 'cross-encoder', 'api']).optional().default('api')`
- Add rationale comment pointing at 12.1g summary.
- Match docker-compose.yml RERANK_TYPE default to `api`.
- Verify tsc clean, tests pass.

## Commit 3 (C3) — baseline + summary + optional default
Files: 2 archive pairs + summary + (optional) src/env.ts + docker-compose.yml.
Message: `Phase 12 Sprint 12.1g — TEI+bge baseline + summary + (conditional) RERANK_TYPE default change`.

## T9 — SESSION_PATCH

Prepend new `CH-PHASE12-S121G` block. Full journey: problem (12.1f confirmed LM Studio rerank limit) → experimental validation → sprint execution → quality result → determinism verdict → decision + recommendation.

## Commit 4 (C4) — SESSION_PATCH
File: docs/sessions/SESSION_PATCH.md.
Message: `Phase 12 Sprint 12.1g SESSION_PATCH (CH-PHASE12-S121G)`.

## Verification gate

- [ ] docker compose config | grep tei-rerank shows service
- [ ] TEI `/health` returns 200
- [ ] TEI `/rerank` smoke test returns meaningful scores
- [ ] npx tsc --noEmit clean
- [ ] npm test passes with new tests
- [ ] Baseline archives exist with per_query.length=40, errors=0
- [ ] Determinism: 0/40 query diffs between T5 and T6
- [ ] Summary doc has top-line recommendation + full matrix

## Estimated time
- T0 pre-flight: 5 min
- T1 docker service + start: 10 min (+ ~4 min model download if not cached)
- T2 env.ts: 2 min
- T3 rerankExternalApi + dispatch: 15 min
- T4 unit tests: 20 min
- T5 baseline (needs image rebuild first): 10 min (rebuild) + 2 min (baseline) = 12 min
- T6 determinism: 5 min
- T7 summary: 15 min
- T8 optional default: 5 min
- T9 SESSION: 10 min

**Total: ~100 min** (~1.5-2h) — borderline L/XL.

## Not doing
- GPU TEI (CPU only for this sprint).
- Multiple model support (one model per TEI instance).
- Infinity / Cohere direct (TEI is chosen).
- Salience math changes.
- Performance tuning beyond CPU defaults.

## Subagent dispatch
NOT using — sequential (infra → code → test → baseline), judgment-heavy analysis.
