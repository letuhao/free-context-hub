# Sprint 15.10 — PLAN

**Date:** 2026-05-21
**Spec:** `docs/specs/2026-05-21-phase-15-sprint-15.10-clarify.md`
**Design:** `docs/specs/2026-05-21-phase-15-sprint-15.10-design.md` rev 2 hash `68eda0a65b55c0b481ab2c886c692a16dd1b69ec`
**Size:** M · 9 tasks · inline

## Tasks

### T1 — Migration 0062
**Path:** `migrations/0062_matrix_levels.sql`
**Body:**
- CREATE TABLE doa_matrix_levels (matrix_id UUID, level TEXT, body_id UUID, PK + FKs + CHECK level enum)
- CREATE INDEX doa_matrix_levels_body_idx
- ALTER TABLE requests ADD COLUMN body_by_level JSONB NULL (F1 fix)
- COMMENTs

**Verify:** psql `\d+ doa_matrix_levels` + `\d requests` shows new column.

### T2 — `doaMatrix.ts:resolveMatrixRow`
**Edit:** extend `MatrixRow` shape: add `body_by_level: Map<string, string>`. SQL gains
LEFT JOIN doa_matrix_levels + json_object_agg with FILTER. Service converts JSON → Map.

### T3 — `requests.ts:submitRequest`
**Edits:**
- Remove §261-267 multi-step counter_sign+collective hard-reject.
- Build `bodyByLevel` Map (preferring matrix table; fallback to single body_id for
  required_level when table empty + 15.8 compat).
- Per-step body resolution loop; throw `missing_collective_body` if a step's
  target_office not in the map.
- Distinct-body check on multi-step counter_sign+collective → throw
  `distinct_body_required` if duplicates.
- INSERT request_steps with per-step body_id.
- Snapshot body_by_level onto `requests.body_by_level` via UPDATE (F1 fix).

### T4 — `requests.ts:applyMotionToStep` lapsed branch (F1+F2 fix)
**Edits:**
- Add `body_by_level: Record<string,string> | null` to the requests SELECT.
- Read `req.body_by_level[newLevel]` for the next-level body.
- Two paths:
  - If body present: re-propose collective. UPDATE step `procedure='collective',
    body_id=<next>, status='motion_proposed', motion_id=NULL, deadline=fresh,
    decided_by=NULL`. appendEvent step_escalated with `escalated_to: 'collective',
    body_id`. proposeStepMotion.
  - Else: degrade to unilateral (15.8 behavior). Update payload field
    `degraded_to: 'unilateral'` → **change to `escalated_to: 'unilateral'`** (F2 fix).

### T5 — Update existing 15.8 test for F2 field-rename
**Path:** `src/services/requests.test.ts:15.8 AC6-lapsed`
**Edit:** assertion was `degraded_to: 'unilateral'`-ish; update to assert
`escalated_to: 'unilateral'`. (The 15.8 test in requests.test.ts checked the
step state changes but maybe not the event payload — verify; only update if
needed.)

### T6 — Tests — `requests.test.ts`
**Tests:**
- AC2 — submit multi-step counter_sign+collective with distinct bodies → succeeds,
  step_index=0 and step_index=1 have DIFFERENT body_ids; both have
  procedure='collective'; step 0 has motion_proposed status; step 1 has pending.
- AC3 — submit multi-step counter_sign+collective with SAME body on 2 levels →
  reject `distinct_body_required`.
- AC4 — submit collective with missing body for one of the route's levels →
  reject `missing_collective_body`.
- AC12 — 15.8 single-step collective backward compat: existing-style matrix row
  (procedure=collective, body_id set, no doa_matrix_levels entries) still works
  per Sprint 15.8 path; step 0 has body_id matching matrix.body_id.

### T7 — Tests — `motions.test.ts`
**Tests:**
- AC5 — Multi-step counter_sign+collective: tally motion 1 carries → step 1
  auto-proposed motion exists under step 1's body (DIFFERENT body); tally motion
  2 carries → request approved; chain task posted.

### T8 — Tests — `coordinationSweep.test.ts`
**Tests:**
- AC6-re-propose — lapsed motion at intermediate collective level + body_by_level
  has next level body → re-propose under next body (NOT degrade); step now
  procedure='collective' at new level with new body_id, new motion_id.
- AC6-degrade-fallback — lapsed motion at intermediate level + body_by_level
  missing next level → degrade to unilateral (15.8-style); event payload now
  carries `escalated_to: 'unilateral'`.

### T9 — VERIFY + LIVE SMOKE
**Run:**
1. `npm test` (expect 651 → ~660+ green)
2. `tsc --noEmit` clean
3. Apply migration 0062 on docker stack + mark in schema_migrations
4. Rebuild mcp+worker
5. Live smoke: insert distinct-body matrix → submit multi-tier collective request →
   each step has different body → tally each in sequence → request approved + 1
   chain task.

## Sign-off

- [ ] T1-T9 complete
- [ ] All ACs pass
- [ ] npm test green
- [ ] tsc clean
- [ ] Live smoke confirms multi-tier flow
