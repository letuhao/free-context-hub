---
id: PLAN-PHASE12-S121F
date: 2026-04-19
module: Phase12-Sprint12.1f
spec_ref: SPEC-PHASE12-S121F
design_ref: DESIGN-PHASE12-S121F
---

# Plan — Phase 12 Sprint 12.1f

## T0 pre-flight
- `docker compose ps` — mcp up
- `lesson_access_log` COUNT = 90
- Verify the 3 cross-encoder models are listed in LM Studio `/v1/models`
- `grep RERANK .env` — confirm RERANK_TYPE/RERANK_MODEL are NOT set in .env (should be commented)

## T1 — docker-compose.yml + C1 commit
- Add `RERANK_TYPE: ${RERANK_TYPE:-generative}` + `RERANK_MODEL: ${RERANK_MODEL:-}` to mcp env block.
- `docker compose config | grep RERANK` → shows both vars with correct defaults.
- Stage: spec + design + plan + docker-compose.yml.
- **C1 commit:** `Phase 12 Sprint 12.1f spec+design+plan + docker-compose RERANK exposure`.

## T2-T6 — sweep runs (one per (rerank_type, model))

For each run:
1. Recreate mcp with env overrides.
2. Wait ready. Verify env via `docker exec mcp node -e "..."`.
3. Run baseline: `npm run qc:baseline -- --tag sprint-12.1f-{name} --samples 1 --surfaces lessons` (background + Monitor).
4. Verify: archive exists, `per_query.length=40`, `errors=0`, `lesson_access_log` COUNT=90.

Run order + configs:

| Run | Tag suffix | Env set |
|---|---|---|
| T2 generative-ref | `generative-ref` | (no RERANK override — uses default generative) |
| T3 bge | `bge` | `RERANK_TYPE=cross-encoder RERANK_MODEL=text-embedding-bge-reranker-v2-m3` |
| T4 gte | `gte` | `RERANK_TYPE=cross-encoder RERANK_MODEL=gte-reranker-modernbert-base` |
| T5 jina | `jina` | `RERANK_TYPE=cross-encoder RERANK_MODEL=text-embedding-jina-reranker-v3` |
| T6 winner-repeat | `{winner}-repeat` | same as winning run from T3-T5 |

Always: `LESSONS_SALIENCE_NO_WRITE=true` (measurement isolation).

Expected runtime per run: generative ~5min, cross-encoders unknown (expect 1-2min each), winner-repeat same as winner.

## T7 — Analysis + summary doc
- Aggregate matrix: 5 runs × (recall@10, MRR, nDCG@5, nDCG@10, elapsed).
- Per-group nDCG@10 (6 groups × 5 runs).
- Determinism check: 0/40 diffs between winner and winner-repeat.
- Decision applied per design §3 matrix.
- Top-line recommendation + decision-support bullets.

## C2 commit — 5 archive pairs + summary
Staged: 10 new baseline files + summary.
Message: `Phase 12 Sprint 12.1f — cross-encoder rerank evaluation (5 runs) + summary`.

## T8 (conditional) — default change
If winner justifies: update `src/env.ts` `RERANK_TYPE` default. Test + tsc clean.
**C3 (conditional):** env default change.

## T9 — SESSION_PATCH
New block with id `CH-PHASE12-S121F`. Include: winner + quality comparison + determinism verdict + recommendation for measurement vs production rerank.
**C4:** SESSION_PATCH.

## Verification gate
- [ ] 5 archives valid (40q, 0 errors each)
- [ ] `lesson_access_log` COUNT = 90 throughout
- [ ] Summary doc has top-line rec + full matrix + per-group + determinism verdict
- [ ] Winner-repeat shows 0/40 diffs
- [ ] tsc clean, tests 231/231

## Estimated total: ~30 min
5 min pre-flight + C1. 20-30 min for 5 baselines. 10 min analysis + C2. 5-10 min session + C4.

## Not doing
- Testing all 9 rerank models (only 3 architecturally-distinct).
- Altering salience/blend/dedup/math.
- Changing DISTILLATION_ENABLED per run.
