# Sprint 15.8 — PLAN

**Date:** 2026-05-20
**Spec:** `docs/specs/2026-05-20-phase-15-sprint-15.8-clarify.md` (approved)
**Design:** `docs/specs/2026-05-20-phase-15-sprint-15.8-design.md` (rev 2, hash `6d145951b9467172b096470e3b73af35d9a313b3`)
**Size:** M · Execution mode: inline · No placeholders

12 tasks (T1–T12).

---

## T1 · Migration 0061
**Path:** `migrations/0061_collective_step.sql`
**Body:** ALTER doa_matrix ADD procedure + body_id + collective_body_ck; ALTER
request_steps ADD body_id + motion_id; DROP+ADD status CHECK to allow 'motion_proposed';
CREATE INDEX request_steps_motion_lookup_idx (sparse).
**Verify:** psql `\d+ request_steps` shows new columns + extended CHECK.

## T2 · `src/services/doaMatrix.ts` — return procedure + body_id
**Edit:** extend `MatrixRow` type; modify `resolveMatrixRow` SELECT to include both
columns.
**Verify:** tsc clean.

## T3 · `src/services/requests.ts` — submitRequest accepts collective
**Edit:**
- Remove §169–171 collective-reject.
- Add §2.2 multi-step counter_sign+collective reject (BAD_REQUEST
  `counter_sign_collective_not_supported`).
- Per-step INSERT now includes `body_id` from matrix.
- After step inserts, if step 0 is collective, call `proposeStepMotion(client, ...)`.
**Verify:** tsc clean.

## T4 · `src/services/requests.ts` — proposeStepMotion helper (new)
**Edit:** new `async function proposeStepMotion(client, args)`. Inserts motions row +
appendEvent motion.proposed + UPDATE request_steps SET status='motion_proposed',
motion_id=<id>. Returns `{motion_id}`.
**Verify:** tsc clean.

## T5 · `src/services/requests.ts` — decideStep early-reject collective
**Edit:** after the `request_steps FOR UPDATE` lock + status validation, branch on
`stepRow.procedure === 'collective'` → ROLLBACK + return `{status:
'procedure_is_collective'}`. Also: add `procedure` to the request_steps SELECT.
**Verify:** existing decideStep tests pass; new test fails until applied.

## T6 · `src/services/requests.ts` — applyMotionToStep (new, exported)
**Edit:** new `export async function applyMotionToStep(client, args)`. Implements the
4 outcome cases per DESIGN §2.8:
- carried → step.endorsed, advance to next step OR finalize (with chain).
- failed → step.returned, request.returned, resolveArtifact(return), request.resolved.
- lapsed → degrade-to-unilateral escalation (F1 fix) OR request.escalation_exhausted.
- vetoed → step.rejected, request.rejected, no chain.

Notes:
- Includes Sprint 15.7 chain handler integration on the carried-final-step path.
- Uses `buildChainedTaskParams` + `emitChain` (already imported).
- Reads the request row FOR UPDATE for kind + subject_id + execution_task on
  carried-final.
- Lapsed-escalation re-uses the existing step row (sets status=pending,
  target_office=newLevel, procedure=unilateral, body_id=NULL, motion_id=NULL,
  deadline=fresh).
**Verify:** tsc clean.

## T7 · `src/services/motions.ts:tallyMotion` — call applyMotionToStep
**Edit:** after the motion.tallied appendEvent (around line 706), look up
`request_steps WHERE motion_id=<this> FOR UPDATE`. If found, call
`applyMotionToStep(client, ...)`. Note: import from `./requests.js` — circular dep
risk (requests.ts imports from chaining.ts; motions.ts imports from chaining.ts; no
direct cycle). Verify with tsc.
**Verify:** tsc clean.

## T8 · `src/services/coordinationSweep.ts:sweepExpiredMotions` — call applyMotionToStep
**Edit:** mirror T7 logic in the auto-tally branch (around line 482 + lapsed line ~458).
For both auto-tally paths (proposed→lapsed and balloting→outcome), check for linked
step.
**Verify:** tsc clean.

## T9 · Tests — `src/services/requests.test.ts` new section
**Tests:**
- AC1 — submitRequest accepts collective + persists procedure/body_id.
- AC1 negative — submitRequest rejects multi-step counter_sign+collective.
- AC5 — decideStep on a collective step → procedure_is_collective.
- AC4 — at submission of a collective step 0, a motion is auto-proposed + step
  status='motion_proposed' + motion_id set.
**Verify:** these tests pass.

## T10 · Tests — `src/services/motions.test.ts` new section
**Tests:**
- AC6-carried — tallyMotion on a motion linked to step → step endorses + request
  approved + 15.7 chain fires.
- AC6-failed — tallyMotion failed → step.returned + request.returned + artifact
  reverted.
- AC6-vetoed — vetoMotion on a motion linked to step → step.rejected + request.rejected.
**Verify:** pass.

## T11 · Tests — `src/services/coordinationSweep.test.ts` new section
**Tests:**
- AC6-lapsed — sweepExpiredMotions auto-lapses a motion linked to step at a non-top
  level → step escalated, procedure degraded to unilateral at new level, NEW
  deadline, NO new motion proposed.
- AC6-lapsed-at-top — same but step at authority level → escalation_exhausted.
**Verify:** pass.

## T12 · VERIFY + LIVE SMOKE
**Run:** `npm test` (expect 638+12=~650 green); `tsc --noEmit` clean; apply migration
0061 to docker stack + mark applied; rebuild mcp+worker; live smoke: submit a request
with collective procedure, tally the motion as carried, verify chained task on the board.
**Verify:** all green.

---

## Sign-off

- [ ] T1–T12 complete
- [ ] All ACs pass
- [ ] `npm test` green
- [ ] `tsc --noEmit` clean
- [ ] Live smoke confirmed
