# Sprint 15.12 — PLAN

**Design:** rev 2 hash `c98c4af3cb1436842e6da7072ecd9fbfb942dd1d` · Size L · inline

## Tasks

### T1 — `requireResourceScope` + `requireBodyProjectScope` middleware
**Path:** `src/api/middleware/requireResourceScope.ts` (new)
- ScopeEntity union (topic/request/motion/dispute/intake/body/task/artifact) + RESOLVERS map.
- `requireResourceScope(entity, paramName='id')` — auth-off/global short-circuit; DB
  lookup; cross-tenant + unknown → 404 NOT_FOUND.
- `requireBodyProjectScope(bodyField='project_id')` — inject key scope on omission;
  404 on explicit mismatch.
**Verify:** tsc clean.

### T2 — Apply to topics.ts routes
`GET /:id`, `POST /:id/join`, `POST /:id/close`, `POST /:id/grant-level`,
`GET /:id/events`, `GET /:id/stream` → `requireResourceScope('topic')`.

### T3 — Apply to board.ts routes
`/topics/:id/board`, `/topics/:id/tasks` → topic; task/artifact-keyed routes → task or
artifact resolver. Confirm route param names.

### T4 — Apply to requests.ts routes
`/topics/:id/requests` → topic; `/requests/:id/...` → request.

### T5 — Apply to motions.ts routes
`/topics/:id/motions` → topic; `/motions/:id/...` → motion; `/decision-bodies/:id/...`
(incl. proxies) → body; `POST /decision-bodies` → requireBodyProjectScope.

### T6 — Apply to disputes.ts + intake.ts routes
disputes: `/topics/:id/disputes` → topic; `/disputes/:id/...` → dispute.
intake: `POST /api/intake` → requireBodyProjectScope; `/intake/:id/...` → intake;
`/projects/:id/intake` → existing requireScope('id').

### T7 — Test bearerAuth shim: support `x-test-key-scope`
Find the test harness's auth shim (how tests set apiKeyRole/apiKeyName) and add
apiKeyScope from an `x-test-key-scope` header so route tests can exercise the scoped path.

### T8 — `coordinationEvents.ts:replayEvents` tail mode
Add `tail?: boolean`. Tail: `ORDER BY seq DESC LIMIT N`, reverse to ASC; has_more via
EXISTS(seq<min) + short-circuit when returned<N.
**Verify:** tsc + existing replay tests pass (no-tail unchanged).

### T9 — `topics.ts:joinTopic` fresh-join uses tail
Fresh join (`since_seq===0`) → tail mode; re-prime (`since_seq>0`) → forward (unchanged).

### T10 — Tests: requireResourceScope.test.ts (new)
Per-entity: auth-off → pass; global → pass; own-project → pass; cross-tenant → 404;
unknown → 404. Plus requireBodyProjectScope: omission injects scope; mismatch → 404.

### T11 — Tests: route-level scoped 404 (a representative subset)
Using `x-test-key-scope`: a project-A key → 404 on a project-B topic close + a project-B
request decide (1-2 representative routes per entity family).

### T12 — Tests: replay tail mode + induction pack
coordinationEvents.test.ts: tail returns recent N seq-ASC; has_more correct.
topics.test.ts: fresh-join pack includes the joiner's own actor_joined (structure on a
small topic; tail==full).

### T13 — VERIFY
`npm test` (expect 680→~700), `tsc --noEmit`, live smoke: scoped key cross-tenant 404
(set a project_scope on a test key + curl) + induction pack on a normal topic.

### T14 — POST-REVIEW light security checklist (§6) + SESSION + COMMIT + RETRO
Walk the 6-point tenant-isolation checklist; mark DEFERRED-009 + 010 RESOLVED; commit;
add lessons; **Phase 15 deferred backlog fully closed.**

## Sign-off
- [ ] T1–T14 · npm test green · tsc clean · live smoke · security checklist CLEAR
