# Sprint 15.11 — PLAN

**Date:** 2026-05-21
**Design:** `docs/specs/2026-05-21-phase-15-sprint-15.11-design.md` rev 2 hash `7226df2e99412ba81dc03e31cd7cbed891edcae3`
**Size:** XL · inline execution with context checkpoints

## Execution order (dependency-aware)

### Phase A — Substrate (T1)
**T1 — Migration 0063** (`migrations/0063_authz_model.sql`):
- `topic_participants.granted_by TEXT NULL`
- `proxies` table (body_id, principal, proxy, granted_by, granted_at, PK + CHECK + index)
- `api_keys.created_by TEXT NULL`
- `api_keys_active_name_uniq` partial unique index
- Pre-flight comment for duplicate-name failure.
Apply to docker stack; verify schemas.

### Phase B — Constants + event type (T2)
**T2 — `coordinationConstants.ts`**: add `'topic.level_granted'` to `EVENT_TYPES`.

### Phase C — Level-grant chain / 015 (T3–T5)
**T3 — `topics.ts:joinTopic`**: load `created_by`; owner-only level honored;
non-owner non-execution → `BAD_REQUEST` (`level_grant_required`); set `granted_by`.
**T4 — `topics.ts:grantLevel`** (new exported fn): owner/authority gate, self-grant
guard, target-participant check, UPDATE level + granted_by, emit `topic.level_granted`.
Add `GrantLevelResult` type.
**T5 — REST + MCP for grant_level**: `POST /api/topics/:id/grant-level` (requireRole
writer) in `routes/topics.ts`; MCP tool `grant_level` in `mcp/index.ts`.

### Phase D — Proxies / 017-Q3 (T6–T8)
**T6 — `proxies.ts`** (new service): `grantProxy` (granted_by===principal gate,
principal-member check, INSERT ON CONFLICT), `revokeProxy`, `listProxies`.
**T7 — `motions.ts:castVote`**: proxy verification gated behind `MCP_AUTH_ENABLED`
(rev 2 F1) — `proxy_not_granted` when auth-on + no grant.
**T8 — REST + MCP for proxies**: `POST/DELETE/GET /api/decision-bodies/:id/proxies`;
MCP `grant_proxy`/`revoke_proxy`/`list_proxies`. Wire `core/index.ts` exports +
`api/index.ts` mounts.

### Phase E — Body authz / 017 (T9–T10)
**T9 — `routes/motions.ts` (or decision-bodies routes)**: raise createBody +
addBodyMember routes from `requireRole('writer')` → `requireRole('admin')`.
**T10 — `decisionBodies.ts:createBody`**: veto_holders length cap (≤64) + element
≤256.

### Phase F — Key provisioning / 016-Q4 (T11–T12)
**T11 — `apiKeys.ts:createApiKey`**: accept `created_by`; catch 23505 on
active-name-uniq → `duplicate_active_key_name`; per-creator count check vs
`MAX_KEYS_PER_CREATOR` → `key_limit_exceeded`.
**T12 — `env.ts`** + api-keys route: add `MAX_KEYS_PER_CREATOR` (default 50); route
passes `created_by` from `req.apiKeyName`.

### Phase G — Test-helper migration (T13) — THE BIG ONE
**T13 — Update shared test helpers** so non-owner participants get levels via the
owner's `grantLevel`, not self-asserted joins:
- `requests.test.ts` `mkTopicWithParticipants`: owner charters + joins authority;
  then grantLevel for coordination/execution actors.
- Same pattern in `motions.test.ts`, `board.test.ts`, `coordinationSweep.test.ts`,
  `topics.test.ts`, `intake.test.ts`, `disputes.test.ts` helpers as needed.
- Run each test file; fix fallout iteratively.

### Phase H — New tests (T14–T16)
**T14 — `topics.test.ts`**: AC1 (joinTopic non-owner forced execution / rejected),
AC2 (owner authority), AC3 (owner grants), AC4 (authority grants), AC5 (coord/exec
can't grant), AC6 (self-grant forbidden), AC7 (decideStep above granted level
impossible).
**T15 — `proxies.test.ts`** (new): grant (principal-only), revoke, list; castVote
proxy verification auth-on (mock env) → proxy_not_granted without grant, success with.
**T16 — `apiKeys.test.ts`**: duplicate-active-name rejected; key_limit_exceeded;
created_by tracked. (Check if a test file exists; create if not.)

### Phase I — Verify + security review (T17–T18)
**T17 — VERIFY**: `npm test` (expect ~657→680+), `tsc --noEmit`, apply migration,
rebuild mcp+worker, live smoke (owner grants authority → grantee decides; proxy grant
+ vote auth-on; key limit).
**T18 — Security-framed cold-start adversarial review** (POST-REVIEW, mandatory per
guardrail 5c0b7b25) — run the §10 checklist with hostile framing. Document verdict.

### Phase J — Close (T19)
**T19 — SESSION + COMMIT + RETRO**: SESSION_PATCH + DEFERRED (015/016/017 RESOLVED) +
commit + add_lesson + AUDIT_LOG.

## Checkpoint note
XL sprint — if context budget tightens mid-BUILD, checkpoint via a partial SESSION
update + commit of completed phases, then resume. Phases A–F are the production code;
G is the test-helper migration; H–I are new tests + verification.

## Sign-off
- [ ] T1–T19 complete
- [ ] npm test green · tsc clean · live smoke · security review CLEAR
