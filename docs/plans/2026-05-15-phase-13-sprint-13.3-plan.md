---
sprint: 13.3
phase: plan
size_class: L
date: 2026-05-15
design_ref: docs/specs/2026-05-15-phase-13-sprint-13.3-design.md (v2, spec_hash 8ac8a0c2e57910f7)
---

# Sprint 13.3 — PLAN: F2 core

Task list (execution order):

1. **migration 0049** — write SQL file with 3 idempotent operations
2. **`src/constants/lessonStatus.ts`** — write file
3. **`src/services/lessons.ts:26`** — extend `LessonStatus` type union
4. **`src/services/activity.ts:7`** — extend `EventType` union
5. **`src/services/reviewRequests.ts`** — write service module (5 fns)
6. **`src/services/reviewRequests.test.ts`** — write 10 tests (ACs 1-7 + concurrency)
7. **`src/api/routes/reviewRequests.ts`** — write REST router
8. **`src/api/index.ts`** — mount reviewRequestsRouter at `/api/projects/:id/review-requests`
9. **`src/core/index.ts`** — re-export reviewRequests service fns
10. **`src/mcp/index.ts`** — 3 read-path enum updates + 1 write-path guard + 2 new MCP tools
11. **`package.json`** — add test file to npm test script
12. **Verify**: `npx tsc --noEmit` clean both backend + gui; `npm test` 279+10=289 pass

Each task: file path concrete, design section is the source of truth. No placeholders. Execution mode: INLINE.
