# Phase 15 Sprint 15.3.1 — Security fix-up — PLAN

- **Date:** 2026-05-18
- **DESIGN:** `docs/specs/2026-05-18-phase-15-sprint-15.3.1-design.md` rev 3 (hash `e8b03d5b5f5b71d2`)
- **Size:** M · 12 tasks · **Execution mode: inline** (main session, sequential — 4 interdependent files, fix-up scale, no subagent dispatch)
- **Files touched:** `src/services/requests.ts`, `src/services/requests.test.ts`,
  `src/api/routes/requests.ts`, `src/api/routes/requests.test.ts`,
  `src/mcp/index.ts` (comment-only).

## Per-file verification commands

- Service file: `npx tsx --test src/services/requests.test.ts`
- Route file: `npx tsx --test src/api/routes/requests.test.ts`
- Full VERIFY: `npm run build` (tsc) + `npm test`

## TDD tasks

### Group A — service-layer fixes (`requests.ts` + `requests.test.ts`)

**T1 — F7 RED.** In `src/services/requests.test.ts`, add test `F7: submitRequest rejects
over-long kind / subject_id` — `mkTopicWithParticipants` + `mkForReviewArtifact`, then two
`assert.rejects`: a `kind` of `'x'.repeat(257)` → `err.code==='BAD_REQUEST'`; and a
`subject_id` of `'y'.repeat(257)` → `err.code==='BAD_REQUEST'`.
*Verify:* run service file → the new test **FAILS** (no length cap exists).

**T2 — F7 GREEN.** In `src/services/requests.ts` `submitRequest`, immediately after the
required-fields check (after line 126, before the B4 weight check), add:
```ts
const MAX_FIELD_LEN = 256;
if (kind.length > MAX_FIELD_LEN || subjectId.length > MAX_FIELD_LEN) {
  throw new ContextHubError('BAD_REQUEST',
    `kind and subject_id must each be at most ${MAX_FIELD_LEN} characters`);
}
```
*Verify:* run service file → T1 test **passes**; all existing 19 tests green.

**T3 — F5 RED.** In `requests.test.ts`, add test `F5: decideStep rejects non-integer /
negative step_index` — submit a valid request, then two `assert.rejects` on `decideStep`:
`step_index: -1` → `BAD_REQUEST`; `step_index: 1.5` → `BAD_REQUEST`.
*Verify:* run service file → **FAILS** (current code returns `not_current_step`, does not throw).

**T4 — F5 GREEN.** In `requests.ts` `decideStep`, after the `request_id`/`actor_id`/`decision`
required-check (after line 349, before `const pool = getDbPool()`), add:
```ts
if (!Number.isInteger(stepIndex) || stepIndex < 0) {
  throw new ContextHubError('BAD_REQUEST', 'step_index must be a non-negative integer');
}
```
*Verify:* run service file → T3 passes; all existing tests green.

**T5 — F3a RED.** In `requests.test.ts`, add test `F3a: submitRequest rejects an artifact
from another topic` (AC3) — charter two topics via two `mkTopicWithParticipants()` calls
(topicA, topicB); `mkForReviewArtifact(topicB, 'doc-xtopic', executionActor)`; call
`submitRequest({ topic_id: topicA, subject_id: <topicB artifact>, kind:'artifact_review',
weight:10, procedure:'unilateral', submitted_by:'execution-actor' })` inside `assert.rejects`
expecting `err.code==='BAD_REQUEST'`... **no** — expecting a `NOT_FOUND` `ContextHubError`
(`err.code==='NOT_FOUND'`).
*Verify:* run service file → **FAILS** (current `SELECT 1 FROM artifacts` ignores topic — no
`NOT_FOUND` thrown).

**T6 — F3a GREEN.** In `src/services/requests.ts`:
1. `submitRequest` artifact check (lines 168-174) → replace with a `SELECT topic_id FROM
   artifacts WHERE artifact_id=$1`; reject when `rowCount===0` **or**
   `rows[0].topic_id !== topicId` → `throw new ContextHubError('NOT_FOUND',
   \`artifact ${subjectId} not found in topic ${topicId}\`)`.
2. `resolveArtifact` (lines 269-321) → drop the `topicId: string` param; change the guarded
   UPDATE to `RETURNING version, content_ref, topic_id`; destructure `topic_id` and use it
   for the two `appendEvent` calls. 0-row early-return is unchanged (returns before the
   destructure).
3. The two `decideStep` call sites (lines 473, 498) → drop the `topicId` argument.
Then add test `F3a: approved request emits artifact events on the artifact topic` (AC4
guard) — submit + endorse-to-approved, `replayEvents({topic_id})`, assert an
`artifact.versioned` and an `artifact.state_changed` event exist with `topic_id === topicId`.
*Verify:* run service file → T5 (AC3) + the AC4 guard pass; existing T9/T10/T11/MED-1 green.

### Group B — route-layer fixes (`routes/requests.ts` + `routes/requests.test.ts`)

**T7 — route test harness.** In `src/api/routes/requests.test.ts`:
1. In `before()`, add a test-shim middleware **before** `app.use('/api', requestsRouter)`:
   ```ts
   app.use((req, _res, next) => {
     const n = req.headers['x-test-key-name'];
     const r = req.headers['x-test-key-role'];
     if (typeof n === 'string' && n) (req as any).apiKeyName = n;
     if (typeof r === 'string' && r) (req as any).apiKeyRole = r;
     next();
   });
   ```
   (Mirrors what `bearerAuth` attaches; inert when the headers are absent.)
2. Extend `request()` with an optional 4th param `headers?: Record<string,string>` merged
   into the request headers.
*Verify:* run route file → all existing 6 tests green (shim inert without headers).

**T8 — F1 RED.** In `routes/requests.test.ts`, add F1 tests:
- `F1: submit with apiKeyName ≠ body submitted_by → 403 IDENTITY_MISMATCH` — header
  `x-test-key-name: execution-actor`, body `submitted_by: coordination-actor` → expect
  `status===403`, `json.code==='IDENTITY_MISMATCH'`.
- `F1: submit with apiKeyName == body submitted_by → 201` — header `x-test-key-name:
  execution-actor`, body `submitted_by: execution-actor` → 201.
- `F1: submit with apiKeyName set, body submitted_by omitted → 201` (uses the key name).
- `F1: decide with apiKeyName ≠ body actor_id → 403 IDENTITY_MISMATCH` (submit first as
  execution-actor, then decide with header name `coordination-actor` + body
  `actor_id: authority-actor`).
- `F1 (AC1b guard): reader-role key POST → 403` — header `x-test-key-role: reader` on a
  POST → expect `status===403` (the existing `requireRole('writer')` gate).
*Verify:* run route file → the IDENTITY_MISMATCH tests **FAIL** (body currently stands → 201).

**T9 — F1 GREEN.** In `src/api/routes/requests.ts`:
1. Add the `resolveActorIdentity(req, bodyValue)` helper (DESIGN §1) + the JSDoc precondition
   note (F1 depends on the writer-role POST gate).
2. `POST /topics/:id/requests` — compute `resolveActorIdentity(req, asString(body.submitted_by))`;
   on `!ok` → `res.status(403).json({status:'error', error:'submitted_by does not match the
   authenticated key', code:'IDENTITY_MISMATCH'})`; else pass `submitted_by: id.actor`.
3. `POST /requests/:id/steps/:n/decide` — same with `body.actor_id`, `code:'IDENTITY_MISMATCH'`.
*Verify:* run route file → T8 F1 tests pass; existing 6 green.

**T10 — F4 RED.** In `routes/requests.test.ts`, add F4 tests:
- `F4: GET /topics/:id/requests with an unknown role → 403` — header `x-test-key-role:
  not-a-real-role` → expect `status===403`.
- `F4: GET /requests/:id with an unknown role → 403` — same.
- `F4: GET with role reader → not 403` — header `x-test-key-role: reader` → `status!==403`.
*Verify:* run route file → the unknown-role tests **FAIL** (no role gate on the GETs → 200).

**T11 — F4 GREEN.** In `src/api/routes/requests.ts`, add `requireRole('reader')` (already
imported) as the second arg to `router.get('/topics/:id/requests', …)` and
`router.get('/requests/:id', …)`.
*Verify:* run route file → T10 passes; all route tests green.

### Group C — MCP doc comment

**T12 — MCP comment.** In `src/mcp/index.ts`, at the
`// ── Phase 15 Sprint 15.3: Request-Approval tools ──` block (~line 3394), append the
identity doc comment from DESIGN §6. Comment-only; no behavioral change, no test.
*Verify:* covered by the VERIFY-phase `tsc`.

## VERIFY phase (after T1–T12)

1. `npm run build` — tsc exit 0.
2. `npm test` — full suite green (414 prior + new F1/F3a/F4/F5/F7 tests).
3. Live smoke (rebuilt `mcp`+`worker` Docker, migration 0056 already live): 14 prior 15.3
   paths + F1 forgery → 403 + F3a cross-topic submit → 404 + F4 unknown-role GET → 403.

## Notes

- No migration, no schema change, no `package.json` change (all 4 code/test files already
  registered in `npm test`).
- `requests.test.ts` `mkTopicWithParticipants()` charters a fresh topic each call →
  T5 calls it twice for two distinct topics; the fixed actor-id strings are fine (the
  `topic_participants` PK is `(topic_id, actor_id)`).
- The test-shim middleware (T7) reproduces `bearerAuth`'s `req.apiKeyName`/`apiKeyRole`
  contract; the real `bearerAuth` is covered by the e2e auth-scope suite, not these unit
  tests.
