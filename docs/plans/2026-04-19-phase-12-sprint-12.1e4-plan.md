---
id: PLAN-PHASE12-S121E4
date: 2026-04-19
module: Phase12-Sprint12.1e4
phase: PHASE_12
spec_ref: SPEC-PHASE12-S121E4
design_ref: DESIGN-PHASE12-S121E4
---

# Plan — Phase 12 Sprint 12.1e4 — α × HL grid sweep

## Pre-flight (T0)

**T0.1 — Verify stack + clean state.**
- `docker compose ps` — services up.
- `curl -sf http://localhost:3001/api/lessons?...` — API responsive.
- `docker compose exec db psql ... "SELECT COUNT(*) FROM lesson_access_log;"` — must be 90 (clean state from 12.1e3 close).
- `npm test` — skip unless suspicious (231/231 from 12.1e3).

**T0.2 — Verify NOWRITE gate is in the container image.**
- Quick smoke: `LESSONS_SALIENCE_NO_WRITE=true docker compose up -d --force-recreate mcp`, run 1 search via curl, check `COUNT(*)` unchanged. Takes ~30s.

## Commit 1 (C1) — docs

**T1.1 — Commit spec + design + plan.**
- Files: 3 new markdown docs.
- Message: `Phase 12 Sprint 12.1e4 spec + design + plan — α × HL grid sweep under NOWRITE`.

## Sweep execution (T2-T9, one task per (HL, α) combination)

For each of 8 combinations in order `[hl=7, α=0.05], [hl=7, α=0.10], [hl=7, α=0.20], [hl=7, α=0.50], [hl=30, α=0.05], [hl=30, α=0.10], [hl=30, α=0.20], [hl=30, α=0.50]`:

**Per-run task (template — 8 instantiations):**
1. Recreate mcp: `LESSONS_SALIENCE_HALF_LIFE_DAYS=<HL> LESSONS_SALIENCE_ALPHA=<α> LESSONS_SALIENCE_NO_WRITE=true docker compose up -d --force-recreate mcp`
2. Wait for ready + verify env (expect 3 env values match expected).
3. Run baseline: `npm run qc:baseline -- --tag sprint-12.1e4-hl<HL>-a<α×100 padded> --samples 1 --surfaces lessons` (background + Monitor).
4. Post-run: confirm archive exists + `per_query.length=40` + `errors=0` + `lesson_access_log COUNT = 90`.

Archive filenames follow: `hl{HL}-a{α×100 as 3-digit zero-pad}` → `hl7-a005, hl7-a010, hl7-a020, hl7-a050, hl30-a005, hl30-a010, hl30-a020, hl30-a050`.

**Estimated runtime per run:** ~5 min (40 queries × 1 sample × ~7s each + ~10s container recreate).
**Estimated total:** ~40-50 min for 8 runs.

## T10 — Analysis + summary doc

**T10.1 — Aggregate metrics matrix (2 tables).**
- Node script reading all 8 JSONs, producing the 2 aggregate tables (one per HL).

**T10.2 — Per-group nDCG@10 (6 groups × 8 runs).**
- Same pattern.

**T10.3 — Per-query notable shifts.**
- Diff found_ranks across the 8 runs. Flag any query where rank of a target changes across α values (within the same HL) — those are where α is actually doing something.

**T10.4 — Write top-line recommendation + summary doc.**
- Commit to an opinion BEFORE writing the data tables.

## Commit 2 (C2) — archives + summary

Files: 16 archive files + `2026-04-19-sprint-12.1e4-summary.md`.
Message: `Phase 12 Sprint 12.1e4 — α × HL grid sweep baselines (8 runs) + summary`.

## T11 (conditional) — α default change

If human at POST-REVIEW picks a new α default:
- `src/env.ts` `LESSONS_SALIENCE_ALPHA` default updated.
- `docker-compose.yml` fallback updated (or left as-is — `:-0.10` was in 12.1e2/e3 commits).
- `npx tsc --noEmit` clean, `npm test` 231/231.

**C3 (conditional):** env default change commit.

## T12 — SESSION_PATCH

**T12.1 — Prepend new block with id `CH-PHASE12-S121E4`.**
Content: sprint outcome, 8-run matrix, top-line recommendation, any default change, next-sprint guidance.

**C4:** SESSION_PATCH commit.

## Verification gate (Phase 6 VERIFY)

- [ ] 8 archives exist, per_query.length=40, errors=0
- [ ] `lesson_access_log` COUNT = 90 throughout (no drift)
- [ ] Summary doc complete with top-line rec + 2 aggregate tables + per-group matrix
- [ ] `npx tsc --noEmit` clean
- [ ] `npm test` 231/231

## Estimated time

- T0 pre-flight: 5 min
- T1 C1 commit: 2 min
- T2-T9 sweep runs: 40-50 min
- T10 analysis + summary: 20 min
- T11 conditional default change: 10 min
- T12 session patch: 10 min

**Total: ~90-100 min** (the 40-50min is mostly waiting on baselines via Monitor).

## Subagent dispatch decision

**NOT using subagent dispatch.** Runs are sequential (container recreates can't parallelize; `--force-recreate mcp` affects the shared container). Analysis is judgment-heavy. Single-context fits.

## Not-doing list

- NOT changing NOWRITE default (stays false for production).
- NOT touching salience math / blend / write paths.
- NOT re-running previous sweeps for comparison — just use 12.1e3's 2×2 as reference.
- NOT pushing until POST-REVIEW completes.
