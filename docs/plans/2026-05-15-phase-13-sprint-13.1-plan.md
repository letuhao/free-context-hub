# Phase 13 Sprint 13.1 — PLAN

**Date:** 2026-05-15
**Design ref:** v2.1 (spec_hash `f14ede2370dcfec5`)
**Size:** L (7 files, ~12 logic changes, side effects YES)

## Task list (sequential — each must verify before next)

### Task 1: Migration 0048

**File:** `migrations/0048_artifact_leases.sql` (NEW)
**Intent:** Table + 3 indexes per design Migration 0048 section.
**Verification:**
```bash
docker compose exec -T db psql -U contexthub -d contexthub -c "\d artifact_leases"
# Expected: table exists with 9 columns + 3 indexes
```

### Task 2: Service module

**File:** `src/services/artifactLeases.ts` (NEW)
**Intent:** Implement 6 functions per design v2.1: claimArtifact (with retry loop), _claimArtifactOnce, fetchConflictResultOrRetry, releaseArtifact, renewArtifact, listActiveClaims, checkArtifactAvailability, forceReleaseArtifact.
**Verification:** `npx tsc --noEmit` clean.

### Task 3: Re-export from core/index.ts

**File:** `src/core/index.ts` (MODIFIED — add 1 line)
**Intent:** Re-export service functions for use by REST routes (matches existing pattern for lessons).
**Diff:**
```typescript
+export { claimArtifact, releaseArtifact, renewArtifact, listActiveClaims, checkArtifactAvailability, forceReleaseArtifact } from '../services/artifactLeases.js';
```
**Verification:** tsc clean.

### Task 4: MCP tools

**File:** `src/mcp/index.ts` (MODIFIED — add ~180 LOC)
**Intent:** 5 `server.registerTool(...)` blocks for claim/release/renew/list/check, mirror existing pattern (e.g., add_lesson at line 700+).
**Verification:** tsc clean; manual MCP call returns expected shape.

### Task 5: REST router

**File:** `src/api/routes/artifactLeases.ts` (NEW)
**Intent:** Express router with `mergeParams: true`. 5 routes: GET /, POST /, GET /:leaseId, PATCH /:leaseId, DELETE /:leaseId, DELETE /:leaseId/force (admin).
**Verification:** tsc clean.

### Task 6: Mount router

**File:** `src/api/index.ts` (MODIFIED — add 2 lines)
**Intent:** Import + mount under `/api/projects/:id/artifact-leases` with writer role.
**Verification:** tsc clean; smoke curl returns 200/404 as expected.

### Task 7: Unit tests

**File:** `src/services/artifactLeases.test.ts` (NEW)
**Intent:** 17 tests per design test plan. Use real DB (test schema OR clean rows in `beforeEach`).
**Verification:** `npm test -- artifactLeases` all pass.

### Task 8: artifact_id convention doc

**File:** `docs/artifact-id-convention.md` (NEW, ~40 lines)
**Intent:** Format, examples, validation regex, agent obligations.
**Verification:** file exists, lints clean.

### Task 9: Apply migration + smoke

**Code:**
```bash
docker compose restart mcp worker
# wait healthy
curl -s -X POST http://localhost:3001/api/projects/free-context-hub/artifact-leases \
  -H "Content-Type: application/json" -H "Authorization: Bearer ..." \
  -d '{"agent_id":"smoke-test","artifact_type":"custom","artifact_id":"smoke/test-1","task_description":"smoke probe"}'
# Expected: {"status":"claimed","lease_id":"...","expires_at":"..."}
```
**Verification:** Smoke calls succeed against running stack.

## Acceptance flow

- Tasks 1-8: tsc clean after each
- Task 7 tests pass before marking BUILD complete
- Task 9 smoke succeeds before marking VERIFY complete

## Estimated remaining cost

- BUILD (Tasks 1-8): ~30K main session tokens
- VERIFY (Task 9 + tsc + tests): ~5K
- REVIEW-CODE: 1-2 Adversary rounds (~80-150K)
- QC + POST-REVIEW (Scope Guard): ~70K
- SESSION + COMMIT + RETRO: ~5K
- **Sprint total remaining: ~200-250K tokens**

Plus already-spent ~150K on CLARIFY/DESIGN/REVIEW-DESIGN. **Total Sprint 13.1: ~350-400K tokens (~$3-5)**.
