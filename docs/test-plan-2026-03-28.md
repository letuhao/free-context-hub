# Test Plan — 2026-03-28

Post-refactor validation for tiered search profiles + priority alignment.

## Test Priority

Follows project priority order: core features first, supplementary second.

---

## 1. Core: Lessons & Knowledge (Must Pass)

### 1.1 Lesson CRUD Lifecycle
```
Test: add_lesson → search_lessons → update_lesson_status → search_lessons
Steps:
  1. add_lesson(type: "decision", title: "Use JWT for auth", content: "Legal requires stateless", tags: ["auth"])
  2. search_lessons(query: "authentication approach") → must find the lesson
  3. search_lessons(query: "JWT vs sessions") → must find the lesson (semantic)
  4. update_lesson_status(lesson_id, status: "superseded", superseded_by: "new-id")
  5. search_lessons(query: "authentication") → superseded lesson should be excluded by default
  6. search_lessons(query: "authentication", filters: {include_all_statuses: true}) → must find it
Pass: all 6 steps return expected results
```

### 1.2 Lesson Types & Tags
```
Test: each lesson_type is searchable and filterable
Steps:
  1. add_lesson(type: "workaround", title: "Redis flush after deploy", tags: ["redis", "deploy"])
  2. add_lesson(type: "preference", title: "Strict TypeScript", tags: ["typescript", "preference-style"])
  3. add_lesson(type: "guardrail", title: "Run tests before push", tags: ["ci"], guardrail: {...})
  4. list_lessons(filters: {lesson_type: "workaround"}) → must contain #1 only
  5. list_lessons(filters: {tags_any: ["redis"]}) → must contain #1 only
  6. search_lessons(query: "deploy caching issue") → must find #1
Pass: filters and search return correct subsets
```

### 1.3 Cross-Session Knowledge Persistence
```
Test: lessons persist and are retrievable after server restart
Steps:
  1. add_lesson(type: "decision", title: "Test persistence", content: "unique-marker-12345")
  2. Record lesson_id
  3. Restart MCP server (docker compose restart mcp)
  4. search_lessons(query: "unique-marker-12345") → must find the lesson
  5. list_lessons() → must contain the lesson
Pass: lesson survives server restart
```

### 1.4 Reflect (if DISTILLATION_ENABLED)
```
Test: reflect synthesizes answer from multiple lessons
Steps:
  1. Add 3 lessons about authentication (JWT decision, session handling workaround, auth middleware preference)
  2. reflect(topic: "how does authentication work in this project")
  3. Response must reference content from multiple lessons
Pass: synthesized answer, not just a single lesson echo
Skip: if DISTILLATION_ENABLED=false
```

---

## 2. Core: Guardrails (Must Pass)

### 2.1 Guardrail Enforcement
```
Test: check_guardrails blocks risky action when guardrail exists
Steps:
  1. add_lesson(type: "guardrail", title: "No push without tests",
     guardrail: {trigger: "git push", requirement: "tests must pass"})
  2. check_guardrails(action_context: {action: "git push"}) → must return pass: false
  3. check_guardrails(action_context: {action: "read file"}) → must return pass: true (no matching guardrail)
Pass: guardrail triggers correctly on matching action, passes on non-matching
```

### 2.2 Guardrail with Superseded Lesson
```
Test: superseded guardrails should not block
Steps:
  1. Add guardrail lesson
  2. check_guardrails → blocks
  3. update_lesson_status(lesson_id, "superseded")
  4. check_guardrails → should pass (guardrail no longer active)
Pass: superseded guardrails are ignored
```

---

## 3. Core: Session Bootstrap (Must Pass)

### 3.1 get_context Returns Useful Bootstrap
```
Test: get_context provides actionable session start data
Steps:
  1. Ensure project has indexed code + some lessons
  2. get_context(task: {intent: "fix auth bug", query: "authentication"})
  3. Response must include: suggested next tool calls, refs to relevant lessons
Pass: non-empty response with actionable suggestions
```

### 3.2 get_project_summary
```
Test: project summary is non-empty and reflects current state
Steps:
  1. get_project_summary(project_id)
  2. Response must include project description, file count, lesson count
Pass: summary reflects actual project state
```

---

## 4. Supplementary: Tiered Search Profiles (Should Pass)

### 4.1 Profile Selection
```
Test: correct profile auto-selected based on kind
Steps:
  1. search_code_tiered(query: "assertWorkspaceToken", kind: "source")
     → response.search_profile must be "code-search"
  2. search_code_tiered(query: "tieredSearch", kind: "test")
     → response.search_profile must be "relationship"
  3. search_code_tiered(query: "how does deployment work", kind: "doc")
     → response.search_profile must be "semantic-first"
  4. search_code_tiered(query: "auth", kind: ["source", "test"])
     → response.search_profile must be "code-search" (mixed fallback)
  5. search_code_tiered(query: "auth")
     → response.search_profile must be "code-search" (default)
Pass: all 5 profile selections correct
```

### 4.2 Code-Search Profile (Regression)
```
Test: code-search behavior unchanged from before refactor
Steps:
  1. search_code_tiered(query: "assertWorkspaceToken", kind: "source")
  2. Must find src/index.ts in results
  3. Tier must be "exact_match" or "symbol_match" (deterministic)
  4. search_code_tiered(query: "parseBooleanEnv", kind: "source")
  5. Must find src/env.ts
Pass: same files found as before, deterministic tiers used
```

### 4.3 Relationship Profile (Test Discovery)
```
Test: kind=test finds test files FOR a function, not just files containing the string
Steps:
  1. search_code_tiered(query: "gitCommitFileParse", kind: "test")
  2. Must find src/services/gitCommitFileParse.test.ts
  3. Tier should be "convention_match" (path convention inference)
  4. search_code_tiered(query: "gitLessonProposalUpsert", kind: "test")
  5. Must find src/services/gitLessonProposalUpsert.test.ts
Pass: convention_match tier finds correct test files
```

### 4.4 Semantic-First Profile (Doc Search)
```
Test: kind=doc uses semantic as primary tier
Steps:
  1. search_code_tiered(query: "how does deployment work", kind: "doc")
  2. Results should have tier="semantic" ranked first
  3. Results should be doc kind files (*.md, README, etc.)
  4. search_code_tiered(query: "project architecture overview", kind: "doc")
  5. Should find WHITEPAPER.md or similar
Pass: semantic tier dominates results for doc queries
```

### 4.5 Kind Filter Accuracy
```
Test: kind filter correctly excludes non-matching files
Steps:
  1. search_code_tiered(query: "auth", kind: "source") → no .md files in results
  2. search_code_tiered(query: "auth", kind: "doc") → no .ts source files in results
  3. search_code_tiered(query: "auth", kind: "config") → only config files (.env, .json, .yaml)
  4. search_code_tiered(query: "chunks", kind: "migration") → only migration SQL files
Pass: each kind returns only matching file types
```

### 4.6 includeTests Auto-Enable
```
Test: kind=test automatically includes test files
Steps:
  1. search_code_tiered(query: "parse", kind: "test")
     → must return test files (includeTests auto-enabled)
  2. search_code_tiered(query: "parse")
     → test files should be excluded by default
Pass: test inclusion logic works correctly
```

### 4.7 Warnings & Degradation
```
Test: warnings field populated when tiers fail
Steps:
  1. search_code_tiered(query: "test", debug: true)
  2. Response must have warnings: [] (empty if all tiers healthy)
  3. Response must have explanations with debug info
Pass: warnings/explanations fields present and correct type
```

---

## 5. Supplementary: Search Quality (QC Regression)

### 5.1 Golden Set Regression
```
Test: existing 51 golden queries don't regress
Steps:
  1. npm run qc:rag (or qc:rag:phase6)
  2. Compare recall@3 and MRR against baseline: recall@3 >= 0.731, MRR >= 0.673
Pass: metrics at or above baseline
Note: this tests search_code (legacy), not search_code_tiered
```

### 5.2 Tiered Search vs Golden Set (New Baseline)
```
Test: run golden queries through search_code_tiered and measure
Steps:
  1. For each golden query, call search_code_tiered(query, kind: "source")
  2. Compute recall@3 and MRR against target_files
  3. Record as new baseline for tiered search
Pass: establish baseline (no target yet — this is the first measurement)
```

---

## Execution Order

| Priority | Test | Requires |
|----------|------|----------|
| P0 | 1.1 Lesson CRUD | MCP server + DB |
| P0 | 1.2 Lesson Types & Tags | MCP server + DB |
| P0 | 2.1 Guardrail Enforcement | MCP server + DB |
| P0 | 3.1 get_context | MCP server + DB + indexed code |
| P1 | 1.3 Cross-Session Persistence | MCP server + Docker restart |
| P1 | 2.2 Guardrail Superseded | MCP server + DB |
| P1 | 3.2 get_project_summary | MCP server + DB |
| P1 | 4.1 Profile Selection | MCP server + indexed code |
| P1 | 4.2 Code-Search Regression | MCP server + indexed code |
| P1 | 4.3 Relationship Profile | MCP server + indexed code |
| P2 | 4.4 Semantic-First Profile | MCP server + indexed code + embeddings |
| P2 | 4.5 Kind Filter Accuracy | MCP server + indexed code |
| P2 | 4.6 includeTests Auto-Enable | MCP server + indexed code |
| P2 | 4.7 Warnings & Degradation | MCP server |
| P2 | 1.4 Reflect | MCP server + DISTILLATION_ENABLED |
| P3 | 5.1 Golden Set Regression | MCP server + full QC infra |
| P3 | 5.2 Tiered Baseline | MCP server + full QC infra |

## Prerequisites

- MCP server running: `docker compose up -d` or `npm run dev`
- Embeddings server running (LM Studio or compatible)
- Project indexed: `index_project(project_id: "free-context-hub", root: "<repo-path>")`
- Redis flushed if retrieval logic changed: `docker compose exec redis redis-cli FLUSHALL`
